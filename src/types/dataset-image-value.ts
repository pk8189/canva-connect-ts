import { types } from "@sideko/canva_connect_api";

export type DatasetImageValue = {
  /**
   * &#x60;asset_id&#x60; of the image to insert into the template element.
   */
  asset_id: string;
  type: types.DatasetImageValueTypeEnum;
};
